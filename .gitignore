__pycache__/
*.pyc
# real scan outputs — may contain client network data, never commit
*.json
!sample-snapshot.json

# auto-submit config (contains the intake secret)
submit.conf
agent.conf
agent-identity.json
